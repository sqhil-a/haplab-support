# HapLab Support

Static support site for HapLab.

## GitHub Pages

Create a new repository, add these files to the repository root, then enable GitHub Pages for the `main` branch.

Use the resulting GitHub Pages URL as the App Store Connect Support URL.
